[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mww-trainer-scripts"
version = "0.1.0"
description = "Minimal microWakeWord training harness: curated data, one venv, one persisted data dir."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["mww_trainer*"]
